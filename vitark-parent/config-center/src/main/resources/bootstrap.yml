#服务端口
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liuzhen1025/repository1
  profiles:
    active: pro
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/register-center/eureka
